{
  "name": "sih-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "backend": "python -m uvicorn backend.app.api.main:app --host 127.0.0.1 --port 8000 --reload"
  }
}
